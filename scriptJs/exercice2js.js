let n = parseInt(prompt("Entrez un nombre"));

for(let i = 1; i <= 10; i++) {

    let result = i * n;

    console.log((n + " * " + i + " = " + n * i));
    window.alert((n + " * " + i + " = " + n * i));
}
